/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    './src/**/*.{html,ts,scss}',
    '../igot-design-system/**/*.{html,ts,scss}',
  ],
  theme: {
    // ── Zoom-Aware Breakpoints ──────────────────────────────
    screens: {
      'xs':     '320px',   // 400% zoom — WCAG Reflow
      'sm':     '426px',   // 300% zoom — Standard Smartphone
      'mobile': '576px',   // ~250% zoom — Mobile ceiling
      'md':     '576px',   // alias
      'phablet':'640px',   // 200% zoom — Phablet
      'lg':     '640px',   // alias
      'xl':     '768px',   // ~166% zoom — Tablet
      'tablet': '992px',   // ~130% zoom — Tablet Landscape
      '2xl':    '853px',   // 150% zoom — Tablet Portrait
      '3xl':    '992px',   // ~130% zoom
      '4xl':    '1024px',  // 125% zoom — Small Desktop
      'desktop':'1200px',  // ~107% zoom — Desktop
      '5xl':    '1200px',  // alias
      '6xl':    '1280px',  // 100% zoom — Base Desktop
      'desktop-lg': '1920px', // Ultra-wide
      '7xl':    '1536px',  // Ultra-wide
    },
    extend: {
      // ── Colors (CSS variable-driven for theming) ──────────
      colors: {
        primary: {
          DEFAULT: 'var(--color-primary)',
          50:  '#e3f2fd',
          100: '#bbdefb',
          200: '#90caf9',
          300: '#64b5f6',
          400: '#42a5f5',
          500: '#2196f3',
          600: '#1e88e5',
          700: '#1976d2',
          800: '#1565c0',
          900: '#0d47a1',
          hover: 'var(--color-primary-hover)',
          light: 'var(--color-primary-light)',
        },
        neutral: {
          0:    '#ffffff',
          50:   '#fafafa',
          100:  '#f5f5f5',
          200:  '#eeeeee',
          300:  '#e0e0e0',
          400:  '#bdbdbd',
          500:  '#9e9e9e',
          600:  '#757575',
          700:  '#616161',
          800:  '#424242',
          900:  '#212121',
          1000: '#000000',
        },
        accent: {
          DEFAULT: 'var(--color-accent)',
          hover: 'var(--color-accent-hover)',
          light: 'var(--color-accent-light)',
        },
        warn: {
          DEFAULT: 'var(--color-warn)',
          hover: 'var(--color-warn-hover)',
          light: 'var(--color-warn-light)',
        },
        success: {
          DEFAULT: 'var(--color-success)',
          light: 'var(--color-success-light)',
          50:  '#dcfce7',
          100: '#bbf7d0',
          600: '#16a34a',
          800: '#166534',
        },
        warning: {
          DEFAULT: '#ff9800',
          50:  '#fff8e1',
          100: '#ffecb3',
          600: '#f57c00',
          800: '#e65100',
        },
        error: {
          DEFAULT: 'var(--color-warn)',
          50:  '#fef2f2',
          100: '#fee2e2',
          600: '#dc2626',
          800: '#991b1b',
        },
        info: {
          DEFAULT: 'var(--color-info)',
          light: 'var(--color-info-light)',
          50:  '#eff6ff',
          100: '#dbeafe',
          600: '#2563eb',
          800: '#1e40af',
        },
        // Semantic bg/text/border via CSS custom properties
        'bg-primary':    'var(--bg-primary)',
        'bg-secondary':  'var(--bg-secondary)',
        'bg-tertiary':   'var(--bg-tertiary)',
        'bg-inverse':    'var(--bg-inverse)',
        'text-primary':  'var(--text-primary)',
        'text-secondary':'var(--text-secondary)',
        'text-tertiary': 'var(--text-tertiary)',
        'text-inverse':  'var(--text-inverse)',
        'border-primary':   'var(--border-primary)',
        'border-secondary': 'var(--border-secondary)',
        'border-focus':     'var(--border-focus)',
      },

      // ── Typography ────────────────────────────────────────
      fontFamily: {
        sans:    ['var(--font-family)'],
        mono:    ['var(--font-family-mono)'],
        primary: ["'Inter'", '-apple-system', 'BlinkMacSystemFont', "'Segoe UI'", 'sans-serif'],
        secondary: ["'Roboto'", 'sans-serif'],
      },
      fontSize: {
        '3xs': ['0.5rem',   { lineHeight: '0.75rem' }],
        '2xs': ['0.625rem', { lineHeight: '0.875rem' }],
        'xs':  ['0.75rem',  { lineHeight: '1rem' }],
        'sm':  ['0.875rem', { lineHeight: '1.25rem' }],
        'base':['1rem',     { lineHeight: '1.5rem' }],
        'lg':  ['1.25rem',  { lineHeight: '1.75rem' }],
        'xl':  ['1.5rem',   { lineHeight: '2rem' }],
        '2xl': ['1.75rem',  { lineHeight: '2.25rem' }],
        '3xl': ['2rem',     { lineHeight: '2.5rem' }],
        '4xl': ['2.25rem',  { lineHeight: '2.75rem' }],
        '5xl': ['2.5rem',   { lineHeight: '3rem' }],
        '6xl': ['2.75rem',  { lineHeight: '3.25rem' }],
        '7xl': ['3rem',     { lineHeight: '3.5rem' }],
      },
      fontWeight: {
        light:     300,
        normal:    400,
        medium:    500,
        semibold:  600,
        bold:      700,
        extrabold: 800,
      },
      lineHeight: {
        none:    '1',
        tight:   '1.25',
        snug:    '1.375',
        normal:  '1.5',
        relaxed: '1.625',
        loose:   '2',
      },

      // ── Spacing (8px grid) ────────────────────────────────
      spacing: {
        '0.5': '0.125rem',
        '1.5': '0.375rem',
        '2.5': '0.625rem',
        '3.5': '0.875rem',
        '18':  '4.5rem',
        '88':  '22rem',
        '128': '32rem',
      },

      // ── Max-Width ─────────────────────────────────────────
      maxWidth: {
        'xs':  '320px',
        'sm':  '426px',
        'md':  '576px',
        'lg':  '640px',
        'xl':  '768px',
        '2xl': '853px',
        '3xl': '992px',
        '4xl': '1024px',
        '5xl': '1200px',
        '6xl': '1280px',
        '7xl': '1536px',
        'container': 'var(--container-max-width)',
      },

      // ── Border Radius ─────────────────────────────────────
      borderRadius: {
        'none':    '0',
        'sm':      '0.125rem',
        'DEFAULT': '0.375rem',
        'md':      '0.5rem',
        'lg':      '0.75rem',
        'xl':      '1rem',
        '2xl':     '1.5rem',
        '3xl':     '1.5rem',
        '4xl':     '2rem',
        'full':    '9999px',
      },

      // ── Box Shadows ───────────────────────────────────────
      boxShadow: {
        'xs':  'var(--shadow-xs)',
        'sm':  '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'DEFAULT': '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)',
        'md':  '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg':  '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        'xl':  '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)',
        '2xl': '0 25px 50px -12px rgba(0, 0, 0, 0.25)',
        'inner': 'inset 0 2px 4px 0 rgba(0, 0, 0, 0.06)',
      },

      // ── Z-Index ───────────────────────────────────────────
      zIndex: {
        'dropdown':       '1000',
        'sticky':         '1020',
        'fixed':          '1030',
        'modal-backdrop': '1040',
        'modal':          '1050',
        'popover':        '1060',
        'tooltip':        '1070',
        'toast':          '1080',
      },

      // ── Animations ────────────────────────────────────────
      animation: {
        'spin-slow':  'spin 3s linear infinite',
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
        'fade-in':    'fadeIn 0.3s ease-in',
        'fade-out':   'fadeOut 0.3s ease-out',
        'slide-up':   'slideUp 0.3s ease-out',
        'slide-down': 'slideDown 0.3s ease-out',
        'scale-in':   'scaleIn 0.2s ease-out',
      },
      keyframes: {
        fadeIn:    { '0%': { opacity: '0' },                                     '100%': { opacity: '1' } },
        fadeOut:   { '0%': { opacity: '1' },                                     '100%': { opacity: '0' } },
        slideUp:   { '0%': { transform: 'translateY(10px)', opacity: '0' },      '100%': { transform: 'translateY(0)', opacity: '1' } },
        slideDown: { '0%': { transform: 'translateY(-10px)', opacity: '0' },     '100%': { transform: 'translateY(0)', opacity: '1' } },
        scaleIn:   { '0%': { transform: 'scale(0.95)', opacity: '0' },           '100%': { transform: 'scale(1)', opacity: '1' } },
      },
    },
  },

  // ── Plugins ─────────────────────────────────────────────
  plugins: [
    // Accessibility utilities plugin
    function ({ addUtilities, addComponents, theme }) {
      // ── Accessibility Utilities ──
      addUtilities({
        '.sr-only': {
          position: 'absolute',
          width: '1px',
          height: '1px',
          padding: '0',
          margin: '-1px',
          overflow: 'hidden',
          clip: 'rect(0, 0, 0, 0)',
          whiteSpace: 'nowrap',
          borderWidth: '0',
        },
        '.sr-only-focusable:focus': {
          position: 'static',
          width: 'auto',
          height: 'auto',
          padding: 'inherit',
          margin: 'inherit',
          overflow: 'visible',
          clip: 'auto',
          whiteSpace: 'normal',
        },
        '.focus-ring': {
          '&:focus-visible': {
            outline: '2px solid var(--border-focus, #2196f3)',
            outlineOffset: '2px',
          },
        },
        '.skip-link': {
          position: 'absolute',
          top: '-40px',
          left: '0',
          background: 'var(--color-primary, #2196f3)',
          color: '#ffffff',
          padding: '0.5rem 1rem',
          textDecoration: 'none',
          zIndex: '9999',
          fontWeight: '600',
          '&:focus': {
            top: '0',
          },
        },
        // Responsive text utilities
        '.text-responsive-sm': {
          fontSize: '0.875rem',
          '@screen mobile': { fontSize: '1rem' },
        },
        '.text-responsive-base': {
          fontSize: '1rem',
          '@screen mobile': { fontSize: '1.25rem' },
        },
        '.text-responsive-lg': {
          fontSize: '1.25rem',
          '@screen mobile': { fontSize: '1.5rem' },
          '@screen tablet': { fontSize: '1.75rem' },
        },
        // Truncation utilities
        '.truncate-2-lines': {
          display: '-webkit-box',
          '-webkit-line-clamp': '2',
          '-webkit-box-orient': 'vertical',
          overflow: 'hidden',
        },
        '.truncate-3-lines': {
          display: '-webkit-box',
          '-webkit-line-clamp': '3',
          '-webkit-box-orient': 'vertical',
          overflow: 'hidden',
        },
      });

      // ── Reusable Component Classes ──
      addComponents({
        // Buttons
        '.btn': {
          display: 'inline-flex',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '0.5rem 1rem',
          borderRadius: '0.375rem',
          fontWeight: '500',
          fontSize: '1rem',
          lineHeight: '1.5rem',
          transition: 'all 200ms ease-in-out',
          cursor: 'pointer',
          '&:focus-visible': {
            outline: '2px solid var(--border-focus, #2196f3)',
            outlineOffset: '2px',
          },
          '&:disabled': {
            opacity: '0.6',
            cursor: 'not-allowed',
          },
        },
        '.btn-primary': {
          backgroundColor: 'var(--color-primary, #2196f3)',
          color: '#ffffff',
          '&:hover:not(:disabled)': {
            backgroundColor: 'var(--color-primary-hover, #1976d2)',
          },
        },
        '.btn-secondary': {
          backgroundColor: '#eeeeee',
          color: '#212121',
          '&:hover:not(:disabled)': {
            backgroundColor: '#e0e0e0',
          },
        },
        '.btn-outline': {
          backgroundColor: 'transparent',
          border: '2px solid var(--color-primary, #2196f3)',
          color: 'var(--color-primary, #2196f3)',
          '&:hover:not(:disabled)': {
            backgroundColor: 'var(--color-primary-light, #e3f2fd)',
          },
        },
        '.btn-ghost': {
          backgroundColor: 'transparent',
          color: 'var(--color-primary, #2196f3)',
          '&:hover:not(:disabled)': {
            backgroundColor: 'var(--color-primary-light, #e3f2fd)',
          },
        },
        '.btn-danger': {
          backgroundColor: 'var(--color-warn, #dc2626)',
          color: '#ffffff',
          '&:hover:not(:disabled)': {
            backgroundColor: 'var(--color-warn-hover, #b91c1c)',
          },
        },
        '.btn-sm': { padding: '0.375rem 0.75rem', fontSize: '0.875rem', minHeight: '2rem' },
        '.btn-lg': { padding: '0.75rem 1.5rem',   fontSize: '1.125rem', minHeight: '3rem' },

        // Cards
        '.card': {
          backgroundColor: 'var(--bg-primary, #ffffff)',
          borderRadius: '0.75rem',
          boxShadow: '0 1px 3px 0 rgba(0,0,0,0.1), 0 1px 2px 0 rgba(0,0,0,0.06)',
          padding: '1.5rem',
        },
        '.card-header': {
          marginBottom: '1rem',
          paddingBottom: '1rem',
          borderBottom: '1px solid var(--border-primary, #e5e7eb)',
        },
        '.card-title': {
          fontSize: '1.25rem',
          fontWeight: '600',
          color: 'var(--text-primary, #111827)',
        },
        '.card-body': {
          color: 'var(--text-secondary, #6b7280)',
        },

        // Form elements
        '.form-label': {
          display: 'block',
          fontSize: '0.875rem',
          fontWeight: '500',
          color: 'var(--text-primary, #111827)',
          marginBottom: '0.5rem',
        },
        '.form-input': {
          width: '100%',
          padding: '0.5rem 0.75rem',
          border: '1px solid var(--border-primary, #e5e7eb)',
          borderRadius: '0.375rem',
          fontSize: '1rem',
          lineHeight: '1.5',
          color: 'var(--text-primary, #111827)',
          backgroundColor: 'var(--bg-primary, #ffffff)',
          transition: 'border-color 200ms, box-shadow 200ms',
          '&:focus': {
            outline: 'none',
            borderColor: 'var(--border-focus, #2196f3)',
            boxShadow: '0 0 0 2px rgba(33, 150, 243, 0.2)',
          },
          '&:disabled': {
            backgroundColor: 'var(--bg-tertiary, #f3f4f6)',
            cursor: 'not-allowed',
          },
        },
        '.form-input-error': {
          borderColor: 'var(--color-warn, #dc2626)',
          '&:focus': {
            boxShadow: '0 0 0 2px rgba(220, 38, 38, 0.2)',
          },
        },
        '.form-help': {
          marginTop: '0.5rem',
          fontSize: '0.875rem',
          color: 'var(--text-secondary, #6b7280)',
        },
        '.form-error': {
          marginTop: '0.5rem',
          fontSize: '0.875rem',
          color: 'var(--color-warn, #dc2626)',
        },

        // Navigation
        '.nav-link': {
          display: 'inline-flex',
          alignItems: 'center',
          padding: '0.5rem 0.75rem',
          borderRadius: '0.375rem',
          fontSize: '0.875rem',
          fontWeight: '500',
          color: 'var(--text-secondary, #6b7280)',
          textDecoration: 'none',
          transition: 'color 200ms, background-color 200ms',
          '&:hover': {
            color: 'var(--color-primary, #2196f3)',
            backgroundColor: 'var(--color-primary-light, #e3f2fd)',
          },
        },
        '.nav-link-active': {
          color: 'var(--color-primary, #2196f3)',
          backgroundColor: 'var(--color-primary-light, #e3f2fd)',
          fontWeight: '600',
        },

        // Badges
        '.badge': {
          display: 'inline-flex',
          alignItems: 'center',
          padding: '0.125rem 0.625rem',
          borderRadius: '9999px',
          fontSize: '0.75rem',
          fontWeight: '500',
        },
        '.badge-primary':  { backgroundColor: '#dbeafe', color: '#1e40af' },
        '.badge-success':  { backgroundColor: '#dcfce7', color: '#166534' },
        '.badge-warning':  { backgroundColor: '#fef3c7', color: '#92400e' },
        '.badge-error':    { backgroundColor: '#fee2e2', color: '#991b1b' },

        // Container
        '.container-custom': {
          width: '100%',
          marginLeft: 'auto',
          marginRight: 'auto',
          paddingLeft: '1rem',
          paddingRight: '1rem',
          '@screen mobile':  { maxWidth: '540px' },
          '@screen tablet':  { maxWidth: '960px' },
          '@screen desktop': { maxWidth: '1140px' },
          '@screen 6xl':     { maxWidth: '1280px' },
        },
      });
    },
  ],
};
