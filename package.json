{
  "name": "igot-mfe-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "iGOT MFE Workspace — Design System, UI Components, Localization & Learner Portal",
  "workspaces": [
    "igot-design-system",
    "igot-ui-components",
    "igot-localization",
    "igot-learner-portal"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace=igot-design-system && npm install --workspace=igot-ui-components && npm install --workspace=igot-localization && npm install --workspace=igot-learner-portal",
    "build:libs": "npm run build --workspace=igot-design-system && npm run build --workspace=igot-ui-components && npm run build --workspace=igot-localization",
    "build:design-system": "npm run build --workspace=igot-design-system",
    "build:ui-components": "npm run build --workspace=igot-ui-components",
    "build:localization": "npm run build --workspace=igot-localization",
    "build:portal": "npm run build --workspace=igot-learner-portal",
    "build:all": "npm run build:libs && npm run build:portal",
    "start": "npm run start --workspace=igot-learner-portal",
    "start:dev": "npm run start:dev --workspace=igot-learner-portal",
    "link:all": "npm run build:libs && npm link ./igot-design-system && npm link ./igot-ui-components && npm link ./igot-localization",
    "dev": "npm run build:libs && npm run start:dev",
    "clean": "rm -rf node_modules igot-*/node_modules igot-*/dist",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@angular/animations": "^20.3.16",
    "@angular/cli": "^20.0.0",
    "@angular/compiler-cli": "^20.0.0",
    "@angular/elements": "^20.3.16",
    "@angular/platform-browser-dynamic": "^20.3.16",
    "@angular/router": "^20.3.16",
    "typescript": "~5.8.0",
    "webpack": "5.105.2"
  },
  "overrides": {
    "webpack": "5.105.2"
  }
}
